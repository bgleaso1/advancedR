# 1 == "1" is true because R will coerce 1 as "1" to make the
# comparison feasible.

# -1 < FALSE is true because FALSE is coerced as 0 to make the
# comparison feasible

# "one" < 2 is false because "o" is greater than "2" in a string
# comparison