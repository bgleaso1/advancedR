# You need to use unlist() because lists can be recursive, which
# unlist() handles but as.vector() does not.

# Lists are technically a type of vector, but they're fundamentally
# different from atomic vectors